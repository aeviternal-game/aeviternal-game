const config = {
    width: 1000,
    height: 1000,
    backgroundColor: 0x000000,
    scene: [ae_scene1, ae_scene2],
    scale: {
        mode: Phaser.Scale.FIT
    }
}


var game = new Phaser.Game(config);